<section class="container mx-auto p-4">
    
    {% if content.columnheading6 is defined %}
        <div class="article-content">
            <h2 class="text-2xl font-bold">{{ content.columnheading6|raw }}</h2>
        </div>
    {% endif %}

    {% if content.articledesc6 is defined %}
        <div class="article-content">
            {{ content.articledesc6|raw }}
        </div>
    {% endif %}
</section>
